# WEBAPP-NAIL DevOps

Development infrastructure is kept here. The current foundation phase provides
PostgreSQL through Docker Compose only.

Use `docker compose -f devops/docker-compose.dev.yml up -d` from the repository
root after creating a local `.env` from `.env.example`.
